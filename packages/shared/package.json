{
  "name": "@snooker/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types and pure business logic for the snooker club management system (pricing engine, billing, ledger, shift reconciliation, idle detection).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured for shared)\""
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
