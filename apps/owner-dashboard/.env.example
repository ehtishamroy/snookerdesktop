# Base URL of the cloud backend (apps/server). The owner dashboard is a pure
# read-mostly client of this API — it never talks to a desktop app directly.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
